%% a)

load("L.mat");

length(L); %temos 90 páginas web

deadends = 0;

for i = 1:length(L)

    if sum(L(:, i)) == 0
        deadends = deadends + 1;
        disp(i);
    end

end

%% b)

%{
H = L./sum(L);
H(isnan(H)) = 0;
%}

H = zeros(length(L));

for i = 1:length(L)
    soma = sum(L(:, i)); % soma das páginas para quais aponta

    if (soma ~= 0)
        H(:, i) = L(:, i) / soma;
    end

end

%% c)

